dist: trusty
language: cpp
compiler:
    - clang
    - gcc
addons:
    apt:
        sources:
            - ubuntu-toolchain-r-test
        package:
            - cmake
            - protobuf
script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build .
