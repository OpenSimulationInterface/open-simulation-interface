dist: trusty
language: cpp
compiler:
    - clang
    - gcc
before_install:
    - sudo apt-get -qq update
    - sudo apt-get install -y cmake protobuf
script:
    - mkdir build
    - cd build
    - cmake ..
    - cmake --build .
